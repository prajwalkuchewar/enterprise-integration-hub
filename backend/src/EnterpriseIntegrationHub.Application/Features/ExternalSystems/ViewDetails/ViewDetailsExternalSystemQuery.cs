namespace EnterpriseIntegrationHub.Application.Features.ExternalSystems.ViewDetails
{
    public sealed record ViewDetailsExternalSystemQuery(
        // You can add properties here if needed in the future
        Guid Id
        );
}